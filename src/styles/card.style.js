import { makeStyles } from "@material-ui/core";

const useStylesCard = makeStyles({
  root: {
    maxWidth: 345,
  },
  form: {
    marginBottom: "2%",
  },
  fields: {
    marginBottom: "2%",
  },
});

export default useStylesCard;
